name: Auto Update QA Branch

on:
  schedule:
    - cron: '* * * * *'  # Runs every minute

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout code
        uses: actions/checkout@v2

      - name: Switch to QA branch, pull changes from main
        run: |
          git config user.name "arganitservices"
          git config user.email "arganitservices@gmail.com"
          git branch -M qa-branch
          git pull origin main
          git push origin qa-branch
